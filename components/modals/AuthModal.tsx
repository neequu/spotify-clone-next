'use client';

import Modal from '../Modal';

const AuthModal = () => {
  return (
    <Modal title='Welcome back' description='Login' isOpen onChange={() => {}}>
      auth
    </Modal>
  );
};

export default AuthModal;
